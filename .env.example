# NoteFlow uses no server-side environment variables.
# All configuration is handled in-app.
#
# Gemini AI API Key:
#   Set your Google Gemini API key inside the app via Settings (gear icon in toolbar).
#   Get your free key at: https://aistudio.google.com/apikey
#   The key is stored in localStorage — never committed to source control.
